# CCD Remote Inference (OpenAI-compatible)
#
# 说明：本项目远端推理由后端代理调用外部 LLM API。
# - provider=openai_compatible 或 provider=remote：读取 CCD_REMOTE_* 变量
# - provider=openai / provider=deepseek / provider=xxx：读取 CCD_OPENAI_* / CCD_DEEPSEEK_* / CCD_XXX_* 变量
#
# 用法（bash）：
#   set -a
#   source .env
#   set +a
#   bash scripts/server/run_api.sh 8001

# -------- 通用（provider=openai_compatible / remote）--------
# 例：OpenAI
CCD_REMOTE_BASE_URL=https://api.openai.com
CCD_REMOTE_API_KEY=sk-REPLACE_ME
# 默认 /v1/chat/completions，一般不需要改；中转站若路径不同可改
# CCD_REMOTE_CHAT_PATH=/v1/chat/completions
# CCD_REMOTE_TIMEOUT_S=60
# CCD_REMOTE_MAX_RETRIES=3

# -------- 按 provider 名称分别配置（可选）--------
# 例：DeepSeek（如果你在前端选择 provider=deepseek）
# CCD_DEEPSEEK_BASE_URL=https://api.deepseek.com
# CCD_DEEPSEEK_API_KEY=sk-REPLACE_ME

# 例：自建/中转站（如果你在前端选择 provider=openai）
# CCD_OPENAI_BASE_URL=https://your-proxy.example.com
# CCD_OPENAI_API_KEY=sk-REPLACE_ME
# CCD_OPENAI_CHAT_PATH=/v1/chat/completions

# -------- vLLM（本地/自建 OpenAI-compatible server）--------
# 说明：
# - 前端选择 provider=vllm / vllm0 / vllm1 时，后端会读取 CCD_VLLM_* / CCD_VLLM0_* / CCD_VLLM1_*。
# - 即使你的 vLLM 未开启鉴权（不校验 key），本后端也要求 API_KEY 非空：可填任意占位符。
#
# 单实例（例如：一台机器一个 vLLM server）：
# CCD_VLLM_BASE_URL=http://127.0.0.1:8002
# CCD_VLLM_API_KEY=dummy
# CCD_VLLM_CHAT_PATH=/v1/chat/completions
#
# 单卡双模型（同一张 GPU 上起两个 vLLM server，不同端口）：
# provider=vllm0 -> 8002，provider=vllm1 -> 8003
# CCD_VLLM0_BASE_URL=http://127.0.0.1:8002
# CCD_VLLM0_API_KEY=dummy
# CCD_VLLM1_BASE_URL=http://127.0.0.1:8003
# CCD_VLLM1_API_KEY=dummy
